<div class="max-w-4xl mx-auto py-8 px-4">
  
  <div class="md:flex md:items-center md:justify-between mb-8">
    <div class="flex-1 min-w-0">
      <h2 class="text-2xl font-bold leading-7 text-gray-900 sm:text-3xl sm:truncate">
        📂 Gerenciar Categorias
      </h2>
      <p class="mt-1 text-sm text-gray-500">
        Crie ou edite as categorias para organizar suas despesas.
      </p>
    </div>
    <div class="mt-4 flex md:mt-0 md:ml-4">
      <%= link_to new_category_path, class: "inline-flex items-center px-4 py-2 border border-transparent rounded-md shadow-sm text-sm font-medium text-white bg-indigo-600 hover:bg-indigo-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-indigo-500" do %>
        <svg class="-ml-1 mr-2 h-5 w-5" xmlns="http://www.w3.org/2000/svg" viewBox="0 0 20 20" fill="currentColor">
          <path fill-rule="evenodd" d="M10 3a1 1 0 011 1v5h5a1 1 0 110 2h-5v5a1 1 0 11-2 0v-5H4a1 1 0 110-2h5V4a1 1 0 011-1z" clip-rule="evenodd" />
        </svg>
        Nova Categoria
      <% end %>
    </div>
  </div>

  <div class="bg-white shadow overflow-hidden sm:rounded-md border border-gray-200">
    <ul class="divide-y divide-gray-200">
      <% @categories.each do |category| %>
        <li class="px-4 py-4 flex items-center justify-between hover:bg-gray-50 transition duration-150 ease-in-out sm:px-6">
          
          <div class="flex items-center">
            <div class="flex-shrink-0 h-10 w-10 rounded-full bg-indigo-100 flex items-center justify-center text-indigo-600">
              <span class="font-bold text-lg"><%= category.name[0].upcase %></span>
            </div>
            <div class="ml-4">
              <div class="text-lg font-medium text-gray-900"><%= category.name %></div>
            </div>
          </div>

          <div class="flex items-center space-x-2">
            <%= link_to edit_category_path(category), class: "text-gray-400 hover:text-indigo-600 p-2 rounded-full hover:bg-gray-100 transition", title: "Editar" do %>
              <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M11 5H6a2 2 0 00-2 2v11a2 2 0 002 2h11a2 2 0 002-2v-5m-1.414-9.414a2 2 0 112.828 2.828L11.828 15H9v-2.828l8.586-8.586z" />
              </svg>
            <% end %>

            <%= button_to category_path(category), method: :delete, data: { turbo_confirm: "Tem certeza? Se apagar esta categoria, os gastos antigos dela ficarão sem categoria." }, class: "text-gray-400 hover:text-red-600 p-2 rounded-full hover:bg-red-50 transition cursor-pointer bg-transparent border-none", title: "Excluir" do %>
              <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 7l-.867 12.142A2 2 0 0116.138 21H7.862a2 2 0 01-1.995-1.858L5 7m5 4v6m4-6v6m1-10V4a1 1 0 00-1-1h-4a1 1 0 00-1 1v3M4 7h16" />
              </svg>
            <% end %>
          </div>

        </li>
      <% end %>

      <% if @categories.empty? %>
        <li class="px-4 py-8 text-center text-gray-500">
          Você ainda não tem categorias. Clique no botão acima para criar.
        </li>
      <% end %>
    </ul>
  </div>

  <div class="mt-6">
    <%= link_to root_path, class: "flex items-center text-sm text-gray-500 hover:text-gray-900 transition" do %>
      <svg class="mr-2 h-4 w-4" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10 19l-7-7m0 0l7-7m-7 7h18"></path></svg>
      Voltar para o Dashboard
    <% end %>
  </div>
</div>